version: 2.1
orbs:
  snyk: snyk/snyk@1.1.2
jobs:
  build:
    docker:
      - image: 'cimg/node:lts'
    steps:
      - checkout
      - run: npm install ci
      - snyk/scan:
          fail-on-issues: false
          monitor-on-build: false
          token-variable: API_TOKEN
